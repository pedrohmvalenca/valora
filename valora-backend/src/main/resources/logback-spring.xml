<?xml version="1.0" encoding="UTF-8"?>
<!--
  Configuração de logging do VALORA — RNF-0009 + project-context §4.6.

  Comportamento por profile:
    - local : layout texto humano (legível em terminal de dev)
    - !local: layout JSON estruturado via LogstashEncoder
              (homol/prod/test — facilita parsing por Datadog/CloudWatch/etc.)

  MDC keys reservados (slots vazios nesta story; populados em stories futuras):
    - userId  : ID do usuário autenticado — populado pela Story 1.8 (AuditAspect)
    - traceId : ID de correlação de request — slot reservado para
                observabilidade futura (não há story atual prevista)
-->
<configuration>

    <!-- Imports do Spring Boot para integrar com profiles e propriedades -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- ========================================================== -->
    <!-- Profile: local — layout texto humano (DX em terminal)      -->
    <!-- ========================================================== -->
    <springProfile name="local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="br.com.senac.valora" level="DEBUG"/>
    </springProfile>

    <!-- ========================================================== -->
    <!-- Demais profiles (homol/prod/test) — JSON estruturado        -->
    <!-- ========================================================== -->
    <springProfile name="!local">
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- Custom fields fixos: identificam app + permitem agrupar em dashboards -->
                <customFields>{"app":"valora-backend"}</customFields>
                <!-- Inclui MDC keys (userId/traceId) automaticamente quando populados -->
                <includeMdcKeyName>userId</includeMdcKeyName>
                <includeMdcKeyName>traceId</includeMdcKeyName>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>

        <logger name="br.com.senac.valora" level="INFO"/>
    </springProfile>

</configuration>
